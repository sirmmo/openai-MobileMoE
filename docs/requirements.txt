# Pinned below the next major: MkDocs 2.0 removes the plugin system and theme
# overrides with no migration path, which would break this site's build.
mkdocs>=1.6,<2
mkdocs-material>=9.5,<10
pymdown-extensions>=11.0.2,<12
